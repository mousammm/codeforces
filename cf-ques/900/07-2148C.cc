/*
 * Title: Pacer 2026-02-05
 *
 * John 1min to run to the other side of the gym 
 * FJ:
 *   - stay in place
 *   - run to the other side (get +1 point)
 * FJ: will run until the mth minute
 */
#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define ll long long
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()
#define pb push_back
#define _ << " " <<

void solve() {
    ll n, k;
    cin >> n >> k;
}
int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    ll T; cin>>T;
    while (T--) solve();
    return 0;
}
