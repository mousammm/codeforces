/*
 * Title : Number Sprial 2026-02-08
 * col -> x axis (horizontal)
 * row -> y axis (vertical)
 *  
 * given:
 */

#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'
#define pb push_back

void solve() {
    int row, col;
    cin >> row >> col;
}

int32_t main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int tt = 1;
    cin >> tt;
    while (tt--) {
        solve();
    }

    return 0;
}
