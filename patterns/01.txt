Find the min number in a array with index

    ll min = INT_MAX;
    int occ = 0, idx = 0;
    for (ll i=0; i<n; i++) {
        if (min > a[i]) {
            min = a[i];
            occ = 1;
            idx = i;
        }
        else if (min == a[i]) {
            occ++;
        }
    }

# Count the num of operation
